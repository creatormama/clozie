# Environment variables — NEVER upload to GitHub
.env
.env.local
.env*.local

# Expo
.expo/
dist/
node_modules/
build*_logs/

# iOS / Android builds (root-anchored so nested module dirs like
# modules/expo-background-removal/ios are NOT stripped from the EAS upload)
/ios/
/android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Mac
.DS_Store
